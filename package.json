{
  "name": "rpa-prisma-module",
  "version": "2.0.0",
  "description": "Module that encapsulates prisma client to be locally installed and accessible by any microservice",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "npx prisma generate && npx prisma migrate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^4.16.2",
    "dotenv": "^10.0.0",
    "express": "^4.18.2"
  },
  "files": [
    ".env",
    "prisma"
  ],
  "devDependencies": {
    "prisma": "^4.16.2"
  }
}
